import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const getCityNameSlice = createSlice({
  name: "findCountry",
  initialState,
  reducers: {},
});

export default getCityNameSlice.reducer;
